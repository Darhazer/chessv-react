/**
 * @chessv/rules — pluggable Rule subclasses.
 *
 * Ports ChessV.Games/Rules: castling, en passant, promotion variants, drops,
 * extinction, and the ~42 other game-specific rules. Each Rule overrides hooks
 * on the engine's Rule base class.
 *
 * Phase 1 ports the core rules; Phase 3 ports the remainder.
 */

export {};
