/**
 * @chessv/variants — the 160 chess-variant definitions.
 *
 * Ports ChessV.Games: the Abstract/Generic* base classes, every variant class
 * and the Evaluations. The C# [Game] attribute is replaced by a `@registerVariant`
 * decorator feeding `registry.ts`, which the UI catalog reads.
 */

export {};
